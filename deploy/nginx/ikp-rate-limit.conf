# Load once from nginx.conf's http {} block through /etc/nginx/conf.d/*.conf.
# The unique zone name avoids collisions with other projects.
limit_req_zone $binary_remote_addr zone=cheloveckmed_ikp_login:10m rate=10r/m;

